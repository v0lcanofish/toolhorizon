# ToolHorizon 依赖清单
#
# ⚠️ 本项目有意分成【两个 Python 环境】—— 实测踩出来的，不是设计偏好：
#
#   环境 A（跑环境 / 探针 / 扩题器）  必须 Python ≥ 3.10
#       τ-bench 用了 3.10+ 语法（envs/airline/env.py 的 match/case、
#       model_utils 里的 `X | None` 注解），3.9 连 import 都过不去（SyntaxError）。
#       本项目实测用的是 Python 3.13
#
#   环境 B（量 token 长度）  需要 transformers / torch
#       只跑 scripts/token_stats.py，不需要 τ-bench
#
# 两个环境不冲突，只是装的东西不一样。

# ---------- 环境 A：跑环境 / 探针 ----------
pydantic>=2.0            # τ-bench 的 Task / Action 是 pydantic 模型

# ⚠️ tau-bench 不在 PyPI 上，需要自己 clone：
#     git clone https://github.com/sierra-research/tau-bench
#   然后让 env/bootstrap.py 能找到它（见该文件顶部的路径配置）。
#   τ-bench 是 MIT 许可。

# ---------- 环境 B：量 token 长度 / 跑训练 ----------
transformers>=4.40
torch                    # CPU 版即可跑全部自测：pip install torch --index-url .../whl/cpu
peft>=0.10               # LoRA

# ---------- 出图 ----------
matplotlib>=3.5

# ---------- 只在 GPU 机器上装 ----------
# ⚠️ vLLM 的版本必须和 torch / CUDA 对得上，**不要**为了装它去升级 torch。
#    照着 GPU-上卡清单.md 第 2 步走。
# vllm
