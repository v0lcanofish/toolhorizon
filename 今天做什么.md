# AutoDL 操作手册 · 照着一行一行做

> 2026-09-18 ｜ 目标：拿到第一条 GRPO 曲线
> **每一步都写清了：在哪个界面、点什么、粘什么、看到什么算成功。**

---

# 一、文件在哪

## 本地（你的电脑）

| 要用的东西 | 完整路径 |
|---|---|
| **上卡包**（要传上去的） | `C:\Users\20298\Desktop\shixi\代码库\projects\ToolHorizon\dist\toolhorizon_upload.tar.gz` |
| 本手册 | `C:\Users\20298\Desktop\shixi\代码库\projects\ToolHorizon\今天做什么.md` |

## 云上（AutoDL 机器里）

| 位置 | 路径 |
|---|---|
| 项目根目录 | `/root/autodl-tmp/ToolHorizon` |
| τ-bench 环境 | `/root/autodl-tmp/tau-bench` |
| 模型 | `/root/autodl-tmp/models/Qwen2.5-1.5B-Instruct` |
| 产出的 adapter | `/root/autodl-tmp/ToolHorizon/models/adapter_sft` |
| 判据报告 | `/root/autodl-tmp/ToolHorizon/reports/sft_gate_base.json`<br>`/root/autodl-tmp/ToolHorizon/reports/sft_gate_sft.json` |
| 训练产物 | `/root/autodl-tmp/ToolHorizon/runs/vanilla/` |

> ⚠️ 数据盘是 `/root/autodl-tmp`（大），系统盘是 `/root`（只有 30GB）。
> **所有东西都放 `autodl-tmp` 里。**

---

# 二、操作步骤

## 第 0 步 · 看实例状态

**在哪**：AutoDL 控制台 → 左侧「容器实例」→ 找到你的卡

**看什么**：卡片右上角的状态

| 状态 | 怎么做 |
|---|---|
| **运行中** | 如果下面写着「无卡模式」→ 直接第 2 步<br>如果没写「无卡模式」（= 正经 GPU 模式）→ 点「关机」，等 30 秒，再第 1 步 |
| **已关机** | 直接第 1 步 |
| **已释放 / 找不到这张卡** | ⚠️ 跳到最后的「附录 · 实例没了怎么办」 |

---

## 第 1 步 · 用无卡模式开机

**在哪**：卡片上的「开机」按钮

**点什么**：
1. 点「开机」
2. 弹出的窗口里**勾上「无卡模式开机」**
3. 确认

**为什么**：无卡模式 ¥0.1/小时。传文件、解压都不需要显卡，**别用 ¥1.8/小时的 GPU 模式干这个**。

**等什么**：状态变成「运行中」，卡片上出现「无卡模式」字样。约 30 秒。

---

## 第 2 步 · 进 JupyterLab，传包，解压

### 2.1 进 JupyterLab

**在哪**：卡片上的「快捷工具」→ 点「JupyterLab」

**看到什么**：浏览器新开一个标签页，左边是文件树，右边是主区域。

### 2.2 进到正确的目录

左边文件树里，**双击进 `autodl-tmp`**。

> 别传错到 `/root`，那是系统盘，只有 30GB。

### 2.3 拖文件

把本地这个文件**直接从 Windows 资源管理器拖进浏览器窗口**：

```
C:\Users\20298\Desktop\shixi\代码库\projects\ToolHorizon\dist\toolhorizon_upload.tar.gz
```

**看到什么**：文件树里出现 `toolhorizon_upload.tar.gz`，等进度条走完（1.5 MB，几秒）。

### 2.4 开终端

**在哪**：JupyterLab 顶部菜单 `File` → `New` → `Terminal`
（或者主区域 Launcher 里的 `Terminal` 图标）

**看到什么**：出现一个黑色命令行窗口。

### 2.5 解压

粘这三行（一行一行粘，每行按回车）：

```bash
cd /root/autodl-tmp
tar xzf toolhorizon_upload.tar.gz
ls
```

**看到什么**：`ls` 的输出里有 `ToolHorizon` 和 `tau-bench` 两个文件夹。

---

## 第 3 步 · 换成 GPU 模式

**在哪**：JupyterLab 标签页**先别关**。回 AutoDL 控制台标签页。

**点什么**：
1. 点卡片上的「关机」
2. 等状态变成「**已关机**」（约 30 秒，别急着点开机）
3. 点「开机」
4. **这次不要勾「无卡模式」** —— 直接确认

**看到什么**：状态变「运行中」，这次卡片上**没有**「无卡模式」字样。

> 💡 无卡模式下传的文件**是保留的**，不会白传。

---

## 第 4 步 · 恢复环境

**在哪**：重新进 JupyterLab（控制台 → 卡片 → 快捷工具 → JupyterLab）→ 再开一个 Terminal

粘这三行：

```bash
cd /root/autodl-tmp/ToolHorizon
source scripts/env.sh
```

**看到什么 —— 这一串 `✓` 才算成功**：

```
── 环境已就绪 ──────────────────────────────────
  ✓ τ-bench: /root/autodl-tmp/tau-bench
  ✓ tokenizer: /root/autodl-tmp/models/Qwen2.5-1.5B-Instruct
  ✓ 数据: /root/autodl-tmp/ToolHorizon/data
  ✓ GPU: NVIDIA GeForce RTX 4090D, 24564 MiB
────────────────────────────────────────────────
✅ 全部就绪。可以跑训练了：
```

**❌ 如果出现 `✗`** —— **停在这里，别往下走**，把整段（包括 `✗` 那行）复制发我。

---

## 第 5 步 · 量基座（约 10 分钟，¥0.3）

先算账（零成本，5 秒）：

```bash
python scripts/eval_sft_gate.py --dry-run
```

**看到什么**：

```
  分片    ：unseen
      unseen      30 题 × 4 条
  ⏱️  预估 ：约 10.3 分钟 ｜ ≈ ¥0.31
```

再真跑：

```bash
python scripts/eval_sft_gate.py --model "$TOOLHORIZON_TOKENIZER" \
    --out reports/sft_gate_base.json
```

**中间会打印**：`[引擎就绪] 119.8s`，然后等约 8 分钟，最后出结果表。

**看到什么**：

```
🚦 判据（unseen(medium) · pass@1 全对口径 · n=4）
   pass@1 = 0.000 < 0.05 —— SFT 没把通过率抬起来
   结论：立刻降到 easy 档... 
```

> ⚠️ **这一步预期就是 `FAIL_HARD`**（还没做 SFT），**这是正常的，别慌。**

---

## 第 6 步 · SFT 预热（约 15 分钟，¥0.5）

```bash
python -m train.trainer sft --model "$TOOLHORIZON_TOKENIZER" \
    --sft-data data/sft_final.jsonl --out models/adapter_sft
```

**中间会打印**（每 5 步一行）：

```
[trainer] SFT 115 条轨迹
[sft] ep0 step1 loss=3.8123
[sft] ep0 step5 loss=2.3451
...
```

**看到什么 —— 最后这一行才算成功**：

```
[trainer] adapter → /root/autodl-tmp/ToolHorizon/models/adapter_sft
```

**⚠️ 盯 loss**：174 步跑完，loss 要明显比第一行低。
如果最后的 loss 和第一行差不多 → 没在学 → **停下来发我**。

---

## 第 7 步 · 验判据（约 10 分钟，¥0.3）★

```bash
python scripts/eval_sft_gate.py --model "$TOOLHORIZON_TOKENIZER" \
    --adapter models/adapter_sft --out reports/sft_gate_sft.json
```

**看到什么**：

```
🚦 判据（unseen(medium) · pass@1 全对口径 · n=4）
   pass@1 = 0.300，落在判据区间 [0.15, 0.50] 内
   结论：可以往下跑 GRPO
   📊 统计噪声：9/30 题全对，95% CI = [0.167, 0.479]
   ✅ 区间整个落在判据内，结论稳
```

**你现在做两件事**：

1. 看 `pass@1` 在不在 `[0.15, 0.50]`
2. **把两个 JSON 的内容贴给我**：

```bash
cat reports/sft_gate_base.json
cat reports/sft_gate_sft.json
```

**⛔ 不在 `[0.15, 0.50]` 的话 —— 停在这里，别做第 8、9 步。** 发我，我给你换档方案。

---

## 第 8 步 · 彩排 1 轮（约 5 分钟，¥0.2）

**只有我说「过」之后才做这步。**

```bash
python -m train.run --rounds 1 --select train --n 8 --limit 4 \
    --model "$TOOLHORIZON_TOKENIZER" --init-adapter models/adapter_sft \
    --run-name rehearsal
```

**看到什么**：

```bash
ls runs/rehearsal/rollouts/ runs/rehearsal/ckpt/
```

要能看到 `step_000.jsonl` 和 `ckpt_000/`。

---

## 第 9 步 · GRPO 25 轮（约 12 小时，¥21）

### 9.1 先开 tmux 会话

```bash
tmux new -s train
```

按回车后，**屏幕底部会出现一条绿色的状态栏**，左下角写着 `[train] 0:bash*`。

> ⚠️ **看到绿色状态栏 = 你在 tmux 里了。没看到 = 你不在，回到上一步重来。**
> 顺序不能反：**必须先进 tmux，再跑训练命令。**

### 9.2 在 tmux 里跑训练

```bash
cd /root/autodl-tmp/ToolHorizon
source scripts/env.sh

python -m train.run --rounds 25 --select arm_base --n 8 \
    --model "$TOOLHORIZON_TOKENIZER" \
    --init-adapter models/adapter_sft \
    --run-name vanilla
```

**看到什么**：开始打印 `[collect] ...`，然后第一轮跑起来。

### 9.3 挂起（让它在后台跑）

训练跑起来之后，按：

```
Ctrl + B     ← 按住 Ctrl，按一下 B，然后两个键都松开
D            ← 再单独按一下 D
```

**看到什么**：
- 屏幕**闪一下**
- **底部的绿色状态栏消失**
- 回到普通的命令行提示符 `root@container:/root/autodl-tmp/ToolHorizon#`

**上面这三条都出现 = 挂起成功。**

### 9.4 关浏览器

**挂起成功后，可以随便关：**
- 关 JupyterLab 标签页 ✅
- 关整个浏览器 ✅
- 关电脑 ✅

训练在服务器上继续跑。

### 9.5 回来看进度

重开 JupyterLab → 开 Terminal → 粘：

```bash
tmux attach -t train
```

**看到什么**：直接跳回训练界面，屏幕上是你离开后的最新输出。

> **确认它还在跑**（不用 attach 也能查）：
> ```bash
> tmux ls
> ```
> 输出 `train: 1 windows (created ...)` 就说明活着。

### 9.6 ⚠️ 唯一会杀死训练的操作

| 操作 | 后果 |
|---|---|
| 关浏览器 / 关电脑 | ✅ 训练继续 |
| 控制台点「**关机**」 | ❌ **训练立刻停** |
| 控制台点「**释放**」 | ❌ **训练停 + 所有数据删除** |

**12 小时里别点关机。**

---

## 第 10 步 · 收产物 + 关机

等第 9 步跑完（终端打出最后一轮的结果），粘：

```bash
tmux attach -t train          # 确认跑完了

cat reports/sft_gate_base.json
cat reports/sft_gate_sft.json
ls runs/vanilla/
ls runs/vanilla/rollouts/
tail -50 reports/train_metrics.jsonl
```

**把这些输出全部复制发我。**

然后：

**AutoDL 控制台 → 卡片 → 点「关机」**（**不是「释放」**）

---

# 三、出错了贴什么给我

**规则：把终端里红色的那一段，从报错第一行到最后一行的原文，整段复制，发我。**

不要只发"报错了"，也不要自己改代码。

常见的几个：

| 看到这个 | 怎么办 |
|---|---|
| `python: command not found` | 先粘 `source /root/miniconda3/etc/profile.d/conda.sh`，再重跑原命令 |
| `✗ τ-bench 找不到` | 把 `✗` 那几行发我 |
| `✗ tokenizer 找不到` | 把 `✗` 那几行发我 —— 可能要重下模型 |
| vLLM 启动报一堆版本冲突 | 整段发我，**别自己 pip install** |
| `CUDA out of memory` | 先粘 `pkill -f vllm`，还是不行就发我 |
| 显存显示只有 22 GB 出头 | **停下来发我**，显存账要重算 |

---

# 附录 · 实例没了怎么办（已释放）

数据盘被清空了，要从零来一遍：

**1.** 重新租卡：控制台 → 算力市场

| 选什么 | 选成什么 |
|---|---|
| GPU 型号 | **RTX 4090**（不要 4090D） |
| 主机数量 | 1 |
| 镜像 | **PyTorch 2.4.0 ｜ Python 3.12 ｜ CUDA 12.1**<br>（找不到就选 2.1.0 ｜ Python 3.10 ｜ CUDA 12.1）<br>⚠️ **Python 必须 ≥ 3.10** |
| 开机方式 | **勾「无卡模式开机」** |

**2.** 传包 + 解压（同第 2 步）

**3.** 跑初始化：

```bash
cd /root/autodl-tmp/ToolHorizon
bash scripts/autodl_setup.sh
```

约 10~20 分钟。它会自己：查 Python 版本 → 开学术加速 → 装依赖 → 下 Qwen2.5-1.5B（3GB）→ 跑项目自检。

**看到「初始化完成」四个字**，就回到第 3 步继续。
